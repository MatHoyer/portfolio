services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        APP_VERSION: ${APP_VERSION:-}
      secrets:
        - github_token
        - email
    environment:
      APP_VERSION: ${APP_VERSION:-}
    ports:
      - "8080:80"

secrets:
  github_token:
    environment: GITHUB_TOKEN
  email:
    environment: EMAIL
